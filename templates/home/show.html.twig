{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Publication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publication.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ publication.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ publication.createdAt|date('d-m-Y') }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ publication.content }}</td>
            </tr>
            <th>Review :</th>
            <th></th>
            <tr>
            {% for comment in publication.comments %}
                <th></th>
                {# <td>{{ comment.CreatedAt|date('d-m-Y') }}</td></tr> #}
                <td>{{ comment.content }}</td></tr>
            {% endfor %}
            </tr>
            <tr>
                <th>Total Comments :</th>
                <td>{{ publication.comments|length }}</td>
            </tr>
        </tbody>
    </table>
    
    <a href="{{ path('app_homeapp_home') }}">back to list</a>
    {# {% if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a>
    {{ include('article/_delete_form.html.twig') }}
    {% endif %} #}
    <h1>Commenter la publication:</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Submit</button>
    {{ form_end(form) }}
{% endblock %}
